version: '3.8'
services:
   db:
      image: postgres:13
      environment:
         POSTGRES_DB: reservation_db
         POSTGRES_USER: postgres
         POSTGRES_PASSWORD: postgres
      ports:
         - "5432:5432"